import { FormControlDateSortPipe } from './form-control-date-sort.pipe';

describe('FormControlDateSortPipe', () => {
  it('create an instance', () => {
    const pipe = new FormControlDateSortPipe();
    expect(pipe).toBeTruthy();
  });
});
