import { SeasonScrubPipe } from './season-scrub.pipe';

describe('SeasonScrubPipe', () => {
  it('create an instance', () => {
    const pipe = new SeasonScrubPipe();
    expect(pipe).toBeTruthy();
  });
});
